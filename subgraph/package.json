{
  "name": "infraveritas-energy-subgraph",
  "version": "1.0.0",
  "description": "InfraVeritas Energy — indexer for ProofSubmitted events on V3 contract (Sepolia)",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy:studio": "graph deploy --node https://api.studio.thegraph.com/deploy/ infraveritas-energy-sepolia",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 infraveritas-energy-sepolia",
    "create:local": "graph create --node http://localhost:8020/ infraveritas-energy-sepolia",
    "remove:local": "graph remove --node http://localhost:8020/ infraveritas-energy-sepolia"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.0",
    "@graphprotocol/graph-ts": "^0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
