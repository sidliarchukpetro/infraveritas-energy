# Poseidon Parameter Freeze — v1.1

**Status:** v1.1 (2026-05-13 — switched sponge from t=3 r=2 to t=5 r=4 to match Noir library's `pub fn sponge`. Cross-language Python ≡ Noir verified for 5 sponge vectors.)
**Date:** 2026-05-13
**Authors:** Petro + Claude
**Reviewer:** Олександр (cybersecurity sanity check — "are these the standard Circom-compatible BN254 params?")
**Scope:** Cross-language Poseidon hash specification для трьох компонентів InfraVeritas pipeline (edge Python, aggregator TypeScript, Noir v08 circuit)

---

## 1. Decision

**Використовуємо Poseidon (Hades design) на BN254 з Circom-compatible параметрами через Noir's `std::hash::poseidon::bn254` module.**

Це **де facto standard** Poseidon для BN254 — ті самі параметри що:
- Noir stdlib `std::hash::poseidon::bn254::hash_N`
- Circom's `circomlib/poseidon.circom`
- Aragon ZK research reference implementation
- IOTA / Sui blockchain Poseidon precompiles
- Aztec, Polygon zkEVM, Mina (mainnet)

**Не Poseidon2** — хоча Poseidon2 дає ~5% менше constraints, ми платимо це за широкий cross-language library support. Critical для трьох-компонентної consistency requirement.

---

## 2. Parameters (frozen)

| Parameter | Value |
|---|---|
| Hash function | Poseidon (Hades design) |
| Curve | BN254 (alt_bn128) |
| Field size | 21888242871839275222246405745257275088548364400416034343698204186575808495617 |
| S-box exponent (α) | **5** (degree-5 power map) |
| Constants source | Circom-compatible (matches Noir stdlib) |
| State sizes (t) | **2 through 17** (Noir provides hash_1 through hash_16 inputs) |
| Full rounds (r_F) | **8** (4 initial + 4 final) |
| Partial rounds (r_P) | **calibrated per t** per Hades paper §3.2 |
| MDS matrix | Circom-compatible (different per t) |
| Round constants | Circom-compatible (different per t, total = (r_F + r_P) × t values) |

**Critical note on r_P:** Partial round count is **calibrated per state size**, not fixed. Use whatever Noir's `std::hash::poseidon::bn254` provides natively for the chosen t. Common values: t=2 → 56, t=3 → 57, t=4 → 56.

---

## 3. Hashing construction for V3 canonical payload

V3 canonical payload = 56 metadata bytes + 100 readings × 24 bytes = **2456 bytes**.

### Field element encoding

Pack each uint64 into a separate BN254 Field element (Field holds 254 bits, accommodates uint64 trivially).

- Metadata: 7 fields (device_id, session_id, epoch_start_ts, lat_e7, lon_e7, light_level, tamper_flag)
- Readings: 300 fields (100 × 3 values per reading)
- **Total: 307 field elements**

### Construction decision

**Sponge: t=5, rate=4, capacity=1, output position [1]** — matches Noir's `pub fn sponge` у `noir-lang/poseidon v0.3.0`.

**Parameters:**
- State size t=5
- Rate=4 (4 field elements absorbed per permutation)
- Capacity=1
- Initial state: `[0; 5]`
- Permutation: `x5_5_config()` (Circom-compatible Hades, x^5 S-box, 8 full + 60 partial rounds)
- Output: `state[1]` after final permutation

**Absorption logic** (from Noir `fn absorb`):**Constraint cost** (307 field elements): 307 / 4 = 76 + remainder 3 → 77 permutations of x5_5.

**Cross-language verification (2026-05-13):**

Python sponge у `edge/hal/poseidon.py` використовує `circomlibpy._build_poseidon(4, 5, inputs, init)` як permutation primitive. **Bit-exact match з Noir** для 5 test vectors різної довжини {4, 5, 8, 17, 100}. Vectors committed у `docs/specs/poseidon_test_vectors.json`.

**Historical note:** Draft v0.9 specified t=3 r=2 sponge based on theoretical preference. v1.1 switched до t=5 r=4 після виявлення що Noir's library exposes цю construction natively. Та сама Poseidon family — інша state width, той самий security level (Poseidon-128).

---

## 4. Test vectors (TO BE GENERATED at migration time)

**Status: PLACEHOLDER** — actual vectors must be generated by running fresh Noir compilation and recording outputs. Below is the **specification of what vectors to generate**, not their values.

### How to generate authoritative vectors

```bash
# In zk/v08/ (after creating skeleton)
nargo new poseidon_vectors
cd poseidon_vectors
# Edit src/main.nr to use poseidon::bn254::hash_N
nargo execute   # Capture output
```

### Required vectors

For cross-language match verification, generate and record:

1. **hash_1**: Input `[1]` → record output
2. **hash_2**: Input `[1, 2]` → record output
3. **hash_3**: Input `[1, 2, 3]` → record output
4. **hash_16**: Input `[1, 2, ..., 16]` → record output
5. **sponge_N** для різної довжини: {4, 5, 8, 17, 100} field elements — generated, committed у JSON. Covers exact-rate, rate+1, 2×rate, 4×rate+1, mid-size boundary conditions.

Vector #5 is the most important — it's what edge / aggregator / circuit must all produce identically.

Store generated vectors у `docs/specs/poseidon_test_vectors.json`:

```json
{
  "hash_1": {
    "input": ["0x01"],
    "output": "0x..."
  },
  "hash_2": {
    "input": ["0x01", "0x02"],
    "output": "0x..."
  },
  ...
  "canonical_v3_sample": {
    "input_payload_hex": "...",
    "output": "0x..."
  }
}
```

### When this becomes v1.0

After vectors generated and committed, this document promotes from Draft v0.9 → v1.0. Until then, **params are frozen but test vectors are unverified**.

**Phase 1 RESOLVED 2026-05-13:** Spočatku пробували `noir-lang/poseidon v0.1.1` — fail (incompatible з Noir 1.0.0-beta.20). Перейшли на `v0.3.0` — success. Test vectors generated, committed at `docs/specs/poseidon_test_vectors.json`. Details у `zk/circuits/v08_poseidon_vectors/README.md`.

---

## 5. Cross-language implementation

Three components must produce identical hash output. Implementation per component:

### Noir circuit (v08)

```rust
use dep::std::hash::poseidon;
let h = poseidon::bn254::hash_2([a, b]);
// For sponge:
// Manual sponge construction або use third-party library like noir-poseidon від TaceoLabs
```

**Dependency:** Noir compiler ≥ 0.34.0 (per noir-lang/poseidon).

### Edge Python (`edge/hal/canonical.py`)

Replace placeholder. Library candidates (verify against test vector #5 before committing):

1. **`poseidon-hash` PyPI** — pure Python, verify Circom compatibility
2. **`circomlibjs` via subprocess** — Node.js bridge, ugly but guaranteed match
3. **Custom port from circomlib JS source** — ~200 lines, full control

**Selection criteria:** must pass all 5 test vectors from §4.

### Aggregator TypeScript

**Library:** [`circomlibjs`](https://github.com/iden3/circomlibjs) `poseidon` — native fit, identical to Circom params we specified.

```typescript
import { buildPoseidon } from "circomlibjs";
const poseidon = await buildPoseidon();
const h = poseidon([a, b]);
const hashBytes = poseidon.F.toString(h);
```

---

## 6. Migration sequence

### Phase 1: Generate authoritative test vectors (1-2 hours)

1. Create `zk/v08/poseidon_vectors/` Noir skeleton project
2. Implement reference hashes for hash_1..hash_16 plus sponge over canonical payload
3. Run `nargo execute`, capture outputs
4. Save to `docs/specs/poseidon_test_vectors.json`
5. Promote this doc to v1.0

### Phase 2: Edge migration (30 min, depends Phase 1)

1. Add Python Poseidon lib до `edge/pyproject.toml`
2. Replace SHA-256 у `edge/hal/canonical.py::compute_payload_hash`
3. Update test → `test_matches_poseidon_reference` (uses test vectors)
4. Verify all 5 vectors pass
5. Commit

### Phase 3: v08 circuit (3-5 days, depends Phase 2)

1. Implement canonical encoding у Noir matching Python `canonicalize()`
2. Apply Poseidon hash using frozen params
3. Output `payloadHash` як public input
4. Compile, generate HonkVerifier.sol
5. Commit з reference proof generation script

### Phase 4: Aggregator integration (depends Phase 3)

1. Implement Poseidon hash через circomlibjs
2. Verify match з edge-computed hash
3. Wire прокинення hash до circuit як public input
4. End-to-end test через V3 submitProof

---

## 7. References

- **Poseidon paper:** Grassi et al., "Poseidon: A New Hash Function for Zero-Knowledge Proof Systems" (USENIX Security '21). https://eprint.iacr.org/2019/458
- **Hades design:** Grassi et al., "On a Generalization of Substitution-Permutation Networks: The Hades Design Strategy" (Eurocrypt '20). https://eprint.iacr.org/2019/1107
- **Circom reference:** `circomlib/poseidon.circom`. https://github.com/iden3/circomlib
- **Noir stdlib:** `std::hash::poseidon::bn254`. https://noir-lang.org/docs (current version's docs)
- **Aragon ZK research:** "The Poseidon hash function and its implementation in Noir". https://research.aragon.org/poseidon-noir.html
- **Optimized Noir Poseidon:** TaceoLabs `noir-poseidon`. https://github.com/TaceoLabs/noir-poseidon

---

## 8. Security review

Standard BN254 Circom-compatible Poseidon у production:
- Aztec protocol (mainnet)
- Polygon zkEVM (mainnet)
- Mina protocol (mainnet)
- iden3 / Polygon ID

**No known practical attacks** against Poseidon з alpha=5, r_F=8, calibrated r_P, на BN254.

**Олександру sanity check:** "Чи ці параметри відповідають Circom-compatible BN254 standard, чи ми не вигадуємо custom params?" Очікувана відповідь — "так, це standard". Не блокуючий, але корисний second pair of eyes на dependency declaration.

---

## 9. Status

- ✅ Parameter decision: Poseidon (Hades), BN254, Circom-compatible
- ✅ Construction decision: Sponge t=5, rate=4, capacity=1, output[1] (matches Noir `pub fn sponge`)
- ✅ Library selections: `noir-lang/poseidon v0.3.0` для circuit, `circomlibjs` для aggregator
- ✅ Phase 1: Test vectors generated 2026-05-13 — `docs/specs/poseidon_test_vectors.json`
- ✅ Doc promoted to v1.0
- ⏳ Phase 2: Edge Python migration — choose Python Poseidon lib, replace SHA-256, verify test vectors pass
- ⏳ Phase 3: v08 Noir circuit — apply Poseidon hash з frozen params до canonical payload
- ⏳ Phase 4: Aggregator TypeScript Poseidon implementation

---

**Кінець Poseidon parameter freeze v1.0.**
